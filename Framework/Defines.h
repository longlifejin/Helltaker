#pragma once

enum class Origins
{
	// T M B
	// L C R
	NONE = -1, TL, TC, TR, ML, MC, MR, BL, BC, BR, CUSTOM, SELF, BOX, KEY,
};

enum class Sides
{
	None = -1, LEFT, RIGHT, NONE, COUNT,
};


enum class SceneIds
{
	NONE = -1, TITLESCENE, CHAPTER, COLLECT, SCENEDVE1, COUNT,

};